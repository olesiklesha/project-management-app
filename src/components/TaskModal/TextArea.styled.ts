import { styled, TextareaAutosize } from '@mui/material';

export const CustomizedTextareaAutosize = styled(TextareaAutosize)`
  width: 90%;
  font-size: 1rem;
  font-family: Roboto;
  line-height: 1.5;
  padding: 10px;
  resize: none;
  margin-left: 3rem;
  margin-bottom: 0.5rem;
`;
